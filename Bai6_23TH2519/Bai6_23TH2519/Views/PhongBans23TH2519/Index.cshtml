@model IEnumerable<Bai6_23TH2519.Models.PhongBan>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
    int i = 0;
}

<h2>DANH SÁCH PHÒNG BAN</h2>

<p>
    @Html.ActionLink("Thêm mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            STT
        </th>
        <th>
            Mã phòng ban
        </th>

        <th>
            Tên phòng ban
        </th>
        <th>
            Chức năng
        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @{i++;}
                @i
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.id_PhongBan)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TenPhongBan)
            </td>
            <td>
                @Html.ActionLink("Sửa", "Edit", new { id = item.id_PhongBan }) |
                @Html.ActionLink("Xem chi tiết", "Details", new { id = item.id_PhongBan }) |
                @Html.ActionLink("Xóa", "Delete", new { id = item.id_PhongBan })
            </td>
        </tr>
    }

</table>

